require 'rake'
require_relative 'html_generator'

desc "create an instance of html generator and output a html file"
task :execute do
  generator = HTMLGenerator.new
  generator.generate_html_pages
end
